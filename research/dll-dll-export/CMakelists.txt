cmake_minimum_required (VERSION 3.10)

# project name
project("dll-dll-export")

# build target
add_executable(${PROJECT_NAME} "main.cpp")

# add dependent library
add_subdirectory("external/dll-parent")
target_link_libraries(${PROJECT_NAME} PRIVATE dll-parent)