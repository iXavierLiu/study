cmake_minimum_required (VERSION 3.13)

# project name
project ("dll-lib-export")

# build target
add_executable(${PROJECT_NAME} "main.cpp")

# add dependent library
add_subdirectory("external/dll-sub")
target_link_libraries(${PROJECT_NAME} PRIVATE dll-sub)